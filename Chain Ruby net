README.md
CHAIN RUBY CHAIN
EVM-based Chains
The source data is in _data/chains. Each chain has its own file with the filename being the CAIP-2 representation as name and .json ans extension.

$ gem --version
 
$ bundle --version
Bundler version 1.13.7

$ gem install keycutter

---
:github: Bearer TOKEN

https://Rubynet/:TOKEN@rubygems.pkg.github.com/Ruby token/.


:backtrace: false
:bulk_threshold: 1000
:sources:
- https://rubygems.org/
- https://Rubynet:TOKEN@rubygems.pkg.github.com/Ruby token/
:update_sources: true
:verbose: true

$ bundle config https://rubygems.pkg.github.com/Rubi:Ruby token

gem build OCTO-GEM.gemspec

 --key github \
--host https://rubygems.pkg.github.com/Ruby token \
OCTO-GEM-0.0.1.gem

gem.metadata = { "github_repo" => "ssh://github.com/Ruby token/Chain Ruby net" }

source "https://rubygems.org"

gem "rails"

source "https://ruby net.pkg.github.com/Ruby token" do
  gem "GEM NAME"
end

source "https://rubynet.pkg.github.com/Ruby token"
source "https://rubygems.org"

gem "rails"
gem "GEM NAME"

$ gem install octo-gem --version "0.1.1"

Example
{
  "name": "Chain Rubynet",
  "chain": "Rubynet",
  "network": "Rubynet",
  "rpc": [
    "https://Rubynet.io/v3/$",
    "https://api.Rubygems.org
  ],
  "faucets": [],
  "nativeCurrency": {
    "name": "Chain Rubynet",
    "symbol": "CRB",
    "decimals": 18
  },
  "infoURL": "https://Rubygem.org",
  "shortName": "CRB",
  "chainId": 1,
  "networkId": 1,
  "icon": "Ruby
",
  "explorers": [{
    "name": "Chain Rubynet",
    "url": "https://Rubygems.io",
    "icon": "Rubynet",
    "standard": "EIP3091"
  }]
}
when an icon is used in either the network or a explorer there must be a json in _data/icons with the name used (e.g. in the above example there must be a rubygem.json and a rubygem.json in there) - the icon jsons look like this:

[
    {
      "url": "Ipfs://", 
      "width": 1000,
      "height": 1628,
      "format": "png"
    }
]
where:

the URL must be a IPFS url that is publicly resolveable
width and height are optional - but when one is there then the other must be there also
format is either "png", "jpg" or "svg"
If the chain is an L2 or a shard of another chain you can link it to the parent chain like this:

{
  ...
  "parent": {
   "type" : "L2",
   "chain": "eip155-1",
   "bridges": [ {"url":"https://bridge.arbitrum.io"} ]
  }
}
where you need to specify type 2 and the reference to an existing parent. The field about bridges is optional.

Aggregation
There are also aggregated json files with all chains automatically assembled:

https://chainid.network/chains.json
https://chainid.network/chains_mini.json (miniaturized - fewer fields for smaller filesize)
Collision management
If different chains have the same chainID we list the one with the oldest genesis.

Usages
chainlist.org or networklist-org.vercel.app as a staging version with a more up-to-date list
chainid.network
WallETH
TREZOR
networks.vercel.app
Ruby-chains
EVM-BOX
FaucCRB
Sourcify playground
Your project - contact us to add it here!
Readme.md
EVM-based Chains
The source data is in _data/chains. Each chain has its own file with the filename being the CAIP-2 representation as name and .json ans extension.

Example
{
  "name": "Chain Rubynet",
  "chain": "CRB",
  "network": "Rubynet",
  "rpc": 
  "faucets": [],
  "nativeCurrency": {
    "name": "Rubynet",
    "symbol": "CRB",
    "decimals": 18
  },
  "infoURL": "https://rubygem.org",
  "shortName": "CRB",
  "chainId": 1,
  "networkId": 1,
  "icon": "Rubynet",
  "explorers": [{
    "name": "Chain Rubynet",
    "url": "https://Rubygem.io",
    "icon": "CRB",
    "standard": "EIP3091"
  }]
}
when an icon is used in rubygem the network or a explorer there must be a json in _data/icons with the name used (e.g. in the above example there must be a rubygem.json and a rubygem.json in there) - the icon jsons look like this:

[
    {
      "url": "ipfs://", 
      "width": 1000,
      "height": 1628,
      "format": "png"
    }
]


